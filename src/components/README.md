# components

All UI building blocks. Split into two sub-folders:

| Folder | Purpose |
|--------|---------|
| `common/` | Domain-agnostic, reusable components consumed across all roles |
| `layout/` | Chrome components — `AppLayout`, `Sidebar`, `Topbar` |
